{
  "name": "billing-app-root",
  "version": "1.0.0",
  "description": "A professional Billing and Invoice Management Software.",
  "author": "Dhanush-An",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "main": "main.js",
  "scripts": {
    "frontend": "npm start --workspace=frontend",
    "backend": "npm run dev --workspace=backend",
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=backend",
    "build": "npm run build --workspace=frontend",
    "start": "electron .",
    "dev:desktop": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"wait-on http://localhost:5173 && cross-env NODE_ENV=development electron .\"",
    "dist": "electron-builder"
  },
  "build": {
    "appId": "com.billingapp.desktop",
    "productName": "BillingApp",
    "directories": {
      "output": "release"
    },
    "files": [
      "frontend/dist/**/*",
      "main.js",
      "preload.js",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/icon.png"
    }
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^41.2.1",
    "electron-builder": "^26.8.1",
    "wait-on": "^9.0.5"
  }
}
